import type{CashupPreset}from'../types/cashupReport';const iso=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;export function cashupPresetDates(p:CashupPreset){const now=new Date(),s=new Date(now.getFullYear(),now.getMonth(),now.getDate()),e=new Date(s);if(p==='all_time')return{startDate:'',endDate:''};if(p==='yesterday'){s.setDate(s.getDate()-1);e.setDate(e.getDate()-1)}else if(p==='last_7_days')s.setDate(s.getDate()-6);else if(p==='this_month')s.setDate(1);else if(p==='last_month'){s.setMonth(s.getMonth()-1,1);e.setDate(0)}return{startDate:iso(s),endDate:iso(e)}}export const cashupRange=(a:string,b:string)=>!a&&!b?'All time':`${a?a.split('-').reverse().join('/'):'Beginning'} - ${b?b.split('-').reverse().join('/'):'Today'}`;
