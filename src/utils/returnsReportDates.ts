import type { ReturnsReportPreset } from '../types/returnsReport';
const iso=(date:Date)=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;
export function returnsPresetDates(preset:ReturnsReportPreset){const today=new Date();const start=new Date(today.getFullYear(),today.getMonth(),today.getDate());const end=new Date(start);if(preset==='all_time')return{startDate:'',endDate:''};if(preset==='yesterday'){start.setDate(start.getDate()-1);end.setDate(end.getDate()-1)}else if(preset==='last_7_days')start.setDate(start.getDate()-6);else if(preset==='this_month')start.setDate(1);else if(preset==='last_month'){start.setMonth(start.getMonth()-1,1);end.setDate(0)}return{startDate:iso(start),endDate:iso(end)}}
export function displayReturnsDateRange(from:string,to:string){if(!from&&!to)return'All time';const show=(x:string)=>x.split('-').reverse().join('/');return`${from?show(from):'Beginning'} - ${to?show(to):'Today'}`}
