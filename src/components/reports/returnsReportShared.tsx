import type{SalesReturnStatus}from'../../types/salesReturn';import{returnLabel}from'../../utils/returnsReportFormat';export function ReturnStatusBadge({status}:{status:SalesReturnStatus}){const tone=status==='completed'?'bg-emerald-500/15 text-emerald-300':status==='cancelled'?'bg-red-500/15 text-red-300':'bg-amber-500/15 text-amber-300';return <span className={`rounded-full px-2 py-1 text-xs font-semibold ${tone}`}>{returnLabel(status)}</span>}
