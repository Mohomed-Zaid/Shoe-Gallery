import type { SummaryRow } from '../../types/salesReport'; import { formatCurrency } from '../../utils/format';
export function ReportSummaryTable({title,rows,showProfit=true}:{title:string;rows:SummaryRow[];showProfit?:boolean}) {return <div className="glass-card overflow-hidden"><h3 className="p-4 font-semibold text-dashboard-text-primary">{title}</h3><div className="overflow-x-auto"><table className="min-w-[1300px] w-full text-sm"><thead className="bg-dashboard-bg-deep text-left text-dashboard-text-label"><tr>{['Name','Invoices','Qty Sold','Returned','Net Qty','Gross Sales','Discounts','Returns','Net Sales','Paid','Outstanding','Cost','Profit','Margin'].map(h=><th className="p-3" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.key} className="border-t border-white/10 text-dashboard-text-sub"><td className="p-3 font-medium text-dashboard-text-primary">{x.label}</td><td>{x.invoices}</td><td>{x.quantity_sold}</td><td>{x.quantity_returned}</td><td>{x.net_quantity}</td><td>{formatCurrency(x.gross_sales)}</td><td>{formatCurrency(x.discounts)}</td><td>{formatCurrency(x.returns)}</td><td>{formatCurrency(x.net_sales)}</td><td>{formatCurrency(x.amount_paid)}</td><td>{formatCurrency(x.outstanding)}</td><td>{showProfit?(x.cost_of_goods==null?'Not available':formatCurrency(x.cost_of_goods)):'Hidden'}</td><td>{showProfit?(x.gross_profit==null?'Not available':formatCurrency(x.gross_profit)):'Hidden'}</td><td>{showProfit&&x.profit_margin!=null?`${x.profit_margin.toFixed(2)}%`:'—'}</td></tr>)}</tbody></table></div>{rows.length===0&&<p className="p-8 text-center text-dashboard-text-sub">No data available.</p>}</div>}
