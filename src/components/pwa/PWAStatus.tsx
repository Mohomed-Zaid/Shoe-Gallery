import { useEffect,useState } from 'react'; import { RefreshCw,WifiOff } from 'lucide-react'; import { useRegisterSW } from 'virtual:pwa-register/react'; import { Button } from '../ui';
export function PWAStatus(){const[online,setOnline]=useState(navigator.onLine);const{needRefresh:[needRefresh],updateServiceWorker}=useRegisterSW({onRegisterError:error=>console.error('Service worker registration failed',error)});useEffect(()=>{const on=()=>setOnline(true),off=()=>setOnline(false);window.addEventListener('online',on);window.addEventListener('offline',off);return()=>{window.removeEventListener('online',on);window.removeEventListener('offline',off)}},[]);return <>{!online&&<div className="fixed inset-x-0 top-0 z-[100] flex items-center justify-center gap-2 bg-red-700 px-4 py-2 text-sm font-semibold text-white"><WifiOff size={16}/><span>No Internet Connection — Some features are unavailable until the connection is restored.</span></div>}{needRefresh&&<div className="fixed bottom-4 right-4 z-[100] flex items-center gap-3 rounded-xl border border-emerald-400/30 bg-emerald-950 p-3 text-sm text-white shadow-2xl"><span>New version available.</span><Button size="sm" onClick={()=>void updateServiceWorker(true)}><RefreshCw size={14}/>Update Now</Button></div>}</>}
