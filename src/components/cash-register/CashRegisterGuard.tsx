import { useEffect,useState,type ReactNode } from 'react'; import { Navigate,useLocation } from 'react-router-dom'; import { getCurrentRegister } from '../../services/cashRegisterService'; import { LoadingSpinner } from '../ui';
export function CashRegisterGuard({children}:{children:ReactNode}){const location=useLocation();const [open,setOpen]=useState<boolean>();useEffect(()=>{getCurrentRegister().then(x=>setOpen(!!x)).catch(()=>setOpen(false))},[]);if(open===undefined)return <LoadingSpinner/>;if(!open)return <Navigate to="/cash-register" replace state={{from:location.pathname,needsOpen:true}}/>;return <>{children}</>}
