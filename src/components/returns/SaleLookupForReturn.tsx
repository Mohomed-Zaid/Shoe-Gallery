import { Search } from 'lucide-react'; import { Button,Input } from '../ui';
export function SaleLookupForReturn({value,onChange,onSearch,loading}:{value:string;onChange:(v:string)=>void;onSearch:()=>void;loading:boolean}){return <section className="glass-card p-5"><div className="relative z-10"><p className="text-xs font-semibold uppercase tracking-[.16em] text-sky-300">Step 1</p><h2 className="mt-1 text-lg font-semibold">Find the original sale</h2><p className="mt-1 text-sm text-dashboard-text-sub">Search using an invoice number. Only completed and partially returned sales are eligible.</p><div className="mt-4 flex gap-3"><Input placeholder="Enter invoice number" value={value} onChange={e=>onChange(e.target.value)} onKeyDown={e=>e.key==='Enter'&&onSearch()}/><Button disabled={loading} onClick={onSearch}><Search size={16}/>{loading?'Searching…':'Search'}</Button></div></div></section>}
