import { useEffect,useState } from 'react'; import { ArrowLeft } from 'lucide-react'; import { Link,useParams } from 'react-router-dom';
import { Alert,Button,LoadingSpinner,PageHeader } from '../components/ui'; import { getSalesReturn } from '../services/salesReturnService'; import type { SalesReturnRecord } from '../types/salesReturn'; import { formatCurrency,formatDateTime } from '../utils/format'; import { getErrorMessage } from '../utils/errors';
export function SalesReturnDetailsPage(){const{id}=useParams();const[record,setRecord]=useState<SalesReturnRecord>();const[loading,setLoading]=useState(true);const[error,setError]=useState<string>();useEffect(()=>{if(id)getSalesReturn(id).then(setRecord).catch(e=>setError(getErrorMessage(e))).finally(()=>setLoading(false))},[id]);if(loading)return <LoadingSpinner/>;if(!record)return <Alert message={error||'Return not found.'}/>;const value=record.sales_return_items.reduce((s,i)=>s+Number(i.return_total),0);return <div className="space-y-6"><PageHeader title={record.return_number} description="Return details" action={<Link to="/returns"><Button variant="secondary"><ArrowLeft size={16}/>Back</Button></Link>}/>{error&&<Alert message={error}/>}<section className="glass-card p-5"><div className="relative z-10 grid gap-3 text-sm sm:grid-cols-2"><p>Original invoice: {record.sale?.invoice_number||'—'}</p><p>Date: {formatDateTime(record.return_date)}</p><p>Customer: {record.customer?.name||'Walk-in Customer'}</p><p>Return value: {formatCurrency(value)}</p><p>Refund: {formatCurrency(Number(record.refund_amount))}</p><p>Status: {record.status}</p></div></section></div>}
