import{jsPDF}from'jspdf';import autoTable from'jspdf-autotable';import type{ProfitDimension,ProfitExportData,ProfitFilters,ProfitInvoiceRow}from'../types/profitReport';import{downloadBlob}from'./salesReportService';import{profitRange}from'../utils/profitReportDates';const q=(v:unknown)=>`"${String(v??'').replaceAll('"','""')}"`,date=(v:string)=>new Date(v).toLocaleDateString('en-GB'),money=(v:number|null)=>v===null?'Unavailable':`LKR ${v.toLocaleString('en-LK',{minimumFractionDigits:2,maximumFractionDigits:2})}`;const ih=['Invoice','Date','Customer','Items','Qty','Gross Sales','Discount','Net Sales','COGS','Profit','Margin','Payment Method','Cashier','Status'],ir=(r:ProfitInvoiceRow):Array<string|number>=>[r.invoice_number,date(r.sale_date),r.customer_name,r.item_count,r.quantity,r.gross_sales,r.discount,r.net_sales,r.cogs??'',r.profit??'',r.margin,r.payment_method,r.cashier_name,r.status];const dh=['Name','Article','Size','Colour','Barcode','Qty','Revenue','COGS','Profit','Margin'],dr=(r:ProfitDimension):Array<string|number>=>[r.product_name||r.name||'',r.article||'',r.size||'',r.colour||'',r.barcode||'',r.quantity,r.revenue,r.cogs??'',r.profit??'',r.margin];const csv=(name:string,h:string[],rows:Array<Array<string|number>>)=>downloadBlob(name,'\ufeff'+[h,...rows].map(r=>r.map(q).join(',')).join('\r\n'),'text/csv;charset=utf-8');export const exportProfitCsv=(d:ProfitExportData)=>csv('profit-report.csv',ih,d.rows.map(ir));export const exportProductProfitCsv=(d:ProfitExportData)=>csv('product-profit.csv',dh,d.products.map(dr));export const exportVariantProfitCsv=(d:ProfitExportData)=>csv('variant-profit.csv',dh,d.variants.map(dr));const xml=(v:unknown)=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;'),sheet=(n:string,rows:Array<Array<string|number>>)=>`<Worksheet ss:Name="${n}"><Table>${rows.map(r=>`<Row>${r.map(v=>`<Cell><Data ss:Type="${typeof v==='number'?'Number':'String'}">${xml(v)}</Data></Cell>`).join('')}</Row>`).join('')}</Table></Worksheet>`;export function exportProfitExcel(d:ProfitExportData){const s=d.summary;const summary=[['Metric','Value'],['Net Revenue',s.net_revenue],['COGS',s.cogs??'Unavailable'],['Gross Profit',s.gross_profit??'Unavailable'],['Margin',s.profit_margin],['Discounts',s.discounts],['Returns',s.return_value],['Units',s.units_sold],['Sales',s.completed_sales]];const daily=[['Date','Invoices','Units','Revenue','Discount','Returns','COGS','Profit','Margin'],...d.daily.map(x=>[x.date,x.invoices,x.units,x.revenue,x.discount,x.returns,x.cogs??'',x.profit??'',x.margin])];downloadBlob('profit-report.xls',`<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet">${sheet('Profit Summary',summary)}${sheet('Invoice Profit',[ih,...d.rows.map(ir)])}${sheet('Product Profit',[dh,...d.products.map(dr)])}${sheet('Variant Profit',[dh,...d.variants.map(dr)])}${sheet('Category Profit',[dh,...d.categories.map(dr)])}${sheet('Daily Profit',daily)}</Workbook>`,'application/vnd.ms-excel')}
const head=(doc:jsPDF,d:ProfitExportData,f:ProfitFilters,detail=false)=>{const w=doc.internal.pageSize.getWidth();doc.setFillColor(5,44,36);doc.rect(0,0,w,27,'F');doc.setTextColor(255,255,255);doc.setFont('helvetica','bold');doc.setFontSize(15);doc.text('SHOE GALLERY',12,10);doc.setFontSize(11);doc.text(detail?'Detailed Profit Report':'Profit Report',w-12,10,{align:'right'});doc.setFont('helvetica','normal');doc.setFontSize(8);doc.text(profitRange(f.startDate,f.endDate),12,18);doc.text(`Generated: ${new Date(d.generatedAt).toLocaleString()}`,w-12,18,{align:'right'})};export function downloadProfitPdf(d:ProfitExportData,f:ProfitFilters){const doc=new jsPDF({orientation:'landscape',unit:'mm',format:'a4'});head(doc,d,f);const s=d.summary;autoTable(doc,{startY:32,head:[['Net Revenue','COGS','Gross Profit','Margin','Discounts','Returns','Units']],body:[[money(s.net_revenue),money(s.cogs),money(s.gross_profit),`${s.profit_margin.toFixed(2)}%`,money(s.discounts),money(s.return_value),s.units_sold]],headStyles:{fillColor:[5,44,36]},styles:{fontSize:7}});const y=((doc as unknown as{lastAutoTable?:{finalY:number}}).lastAutoTable?.finalY??45)+5;autoTable(doc,{startY:y,head:[['Invoice','Date','Customer','Qty','Net Sales','COGS','Profit','Margin']],body:d.rows.map(r=>[r.invoice_number,date(r.sale_date),r.customer_name,r.quantity,money(r.net_sales),money(r.cogs),money(r.profit),`${r.margin.toFixed(2)}%`]),headStyles:{fillColor:[5,44,36]},styles:{fontSize:7}});doc.save('profit-report.pdf')}export function downloadDetailedProfitPdf(d:ProfitExportData,f:ProfitFilters){const doc=new jsPDF({orientation:'landscape',unit:'mm',format:'a4'});head(doc,d,f,true);let y=33;for(const inv of d.rows){if(y>170){doc.addPage();head(doc,d,f,true);y=33}doc.setTextColor(15,23,42);doc.setFont('helvetica','bold');doc.setFontSize(9);doc.text(`${inv.invoice_number} · Profit ${money(inv.profit)}`,8,y);autoTable(doc,{startY:y+2,head:[['Product','Article','Size','Colour','Qty','Net Revenue','Cost','Profit','Margin']],body:d.items.filter(i=>i.sale_id===inv.sale_id).map(i=>[i.product_name,i.article||'—',i.size||'—',i.colour||'—',i.quantity,money(i.net_revenue),money(i.total_cost),money(i.profit),`${i.margin.toFixed(2)}%`]),headStyles:{fillColor:[5,44,36]},styles:{fontSize:6.5},pageBreak:'avoid'});y=((doc as unknown as{lastAutoTable?:{finalY:number}}).lastAutoTable?.finalY??y+10)+7}doc.save('profit-report-detailed.pdf')}
